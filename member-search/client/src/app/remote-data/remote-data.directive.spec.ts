import { RemoteDataDirective } from './remote-data.directive';

describe('RemoteDataDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoteDataDirective();
    expect(directive).toBeTruthy();
  });
});
